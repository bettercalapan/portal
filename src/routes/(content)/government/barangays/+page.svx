---
layout: content
title: Barangays
breadcrumbs:
  - name: Government
    url: /(content)/government
---

<script>
	import { barangays } from "$lib/data/government.data";
	import { source } from "$lib/snippets/source.snippet.svelte";
	import Phone from "@lucide/svelte/icons/phone";
	import User from "@lucide/svelte/icons/user";
</script>

# {title}

{barangays.description}

<ul class="barangays">
	{#each barangays.data as barangay (barangay.name)}
		<li>
			<h2>{barangay.name}</h2>
			<p><User /> {barangay.captain}</p>
			<p><Phone /> {barangay.phoneNumber}</p>
		</li>
	{/each}
</ul>

{@render source(barangays.source.description, barangays.source.linkLabel, barangays.source.linkURL)}

<style>
	.barangays {
		padding: 0;
		margin-top: 2rem;
		gap: 0.5rem;
		list-style: none;
	}

	.barangays > li {
		padding: 1.5rem 1.75rem;
		background-color: var(--neutral-1);
		border-radius: 2rem;
	}

	.barangays h2 {
		margin: 0 0 0.5rem;
		font-size: 1.25rem;
		font-weight: 600;
	}

	.barangays p {
		margin: 0;
		display: grid;
		grid-template-columns: 18px 1fr;
		align-items: center;
		gap: 0.75rem;
	}

	@media (min-width: 900px) {
		.barangays > li {
			padding: 0.5rem 0;
			display: grid;
			grid-template-columns: repeat(3, 1fr);
			align-items: center;
			background: none;
			border-bottom: 1px dotted var(--fg);
			border-radius: 0;
		}
	}
</style>
